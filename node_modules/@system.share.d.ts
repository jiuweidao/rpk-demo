// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * 分享
 * 接口声明: {"name": "system.share"}
 */
export default class share {
    /**
     * 分享数据到其他app
     * @param obj
     * @property type 数据的MIME TYPE，要求字母全小写
     * @property data 分享的数据：1. 如果type是text/开头的mimetype（如text/plain），则data是要分享的文本内容；2. 如果type是其他值，则data是要分享的文件路径。支持三种文件路径：1. 通过fetch.fetch下载的文件路径；2. 通过file.save或list获得的文件路径；3. 以/开头的应用内部的资源文件。
     * @property success 成功回调。因为大部分android app都没有正确的返回分享状态，所以即使分享成功了，也可能执行cancel回调，而不是success回调。
     * @property fail 失败回调
     * @property cancel 取消回调
     * @property complete 执行结束后的回调
     */
    static share(obj: {
        type: string,
        data: string,
        success: Function,
        fail: Function,
        cancel: Function,
        complete: Function
    }): void
}
