// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * 微信账户
 * 接口声明: {"name": "service.wbaccount"}
 */
export default class wbaccount {
    /**
     * 获取当前的微博登录方式
     * @returns APP: SSO授权：在有微博客户端的情况下，同时手机系统支持时，使用SSO授权登陆; WEB: Web授权：不支持SSO授权时，使用webview形式授权; NONE: 当前无可用的微博登陆方式;
     */
    static getType(): any

    /**
     * 发起微信登陆，调用之前应该先使用getType函数查询APP登陆方式是否被支持
     * @param obj
     * @property redirectUri 授权回调地址，与微博开放平台配置保持一致，默认可填写 https://api.weibo.com/oauth2/default.html
     * @property scope 申请scope权限所需参数，可一次申请多个scope权限，用逗号分隔。示例：follow_app_official_microblog
     * @property success 成功回调
     * @property fail 失败回调
     * @property cancel 取消回调
     */
    static authorize(obj: {
        redirectUri: string,
        scope?: string,
        success?: Function,
        fail?: Function,
        cancel?: Function
    }): void
}
