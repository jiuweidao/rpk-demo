// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * QQ账户
 * 接口声明: {"name": "service.qqaccount"}
 */
export default class qqaccount {
    /**
     * 获取当前的QQ登陆方式
     * @returns APP: QQ的app登陆方式; WEB: QQ的网页登陆方式; none: 当前无可用的QQ登陆方式;
     */
    static getType(): any

    /**
     * 发起qq登陆，不同的登陆方式下，应该传入不同的参数，调用之前应该先使用getType函数查询当前的登陆方式．
     * @param obj
     * @property state client端的状态值。用于第三方应用防止CSRF攻击，成功授权后回调时会原样带回。请务必严格按照流程检查用户与state参数状态的绑定。app方式下不使用该参数
     * @property redirectUri 授权回调地址,APP方式下,不使用该参数
     * @property scope 请求用户授权时向用户显示的可进行授权的列表。可填写的值是API文档中列出的接口，以及一些动作型的授权（目前仅有：do_like），如果要填写多个接口名称，请用逗号隔开。
     * @property success 成功回调
     * @property fail 失败回调
     * @property complete 执行结束后的回调
     */
    static authorize(obj: {
        state: string,
        redirectUri: string,
        scope?: string,
        success?: Function,
        fail?: Function,
        complete?: Function
    }): void

    /**
     * 获得用户基本信息
     * @param obj
     * @property token 访问令牌
     * @property success 成功回调
     * @property fail 失败回调
     * @property complete 执行结束后的回调
     */
    static getProfile(obj: {
        token: string,
        success?: Function,
        fail?: Function,
        complete?: Function
    }): void
}
