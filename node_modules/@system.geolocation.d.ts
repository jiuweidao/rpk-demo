// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * 打开网页
 * 接口声明: {"name": "system.geolocation"}
 */
export default class geolocation {
    /**
     * 获取地理位置
     * @param obj
     * @property timeout 设置超时时间，单位是ms，默认值为30000。在权限被系统拒绝或者定位设置不当的情况下，有可能永远不能返回结果，因而需要设置超时。超时后会使用fail回调
     * @property success 成功回调
     * @property fail 失败回调，原因可能是用户拒绝
     * @property complete 执行结束后的回调
     */
    static getLocation(obj: { timeout?: string, success: Function, fail?: Function, complete?: Function }): void

    /**
     * 获取系统当前支持的定位类型
     * @param obj
     * @property success 成功回调
     * @property fail 失败回调
     * @property complete 执行结束后的回调
     */
    static getLocationType(obj: { success: Function, fail?: Function, complete?: Function }): void

    /**
     * 监听地理位置。如果多次调用，仅最后一次调用生效
     * @param obj
     * @property callback 每次位置信息发生变化，都会被回调
     * @property fail 失败回调，原因可能是用户拒绝
     */
    static subscribe(obj: { callback: Function, fail?: Function }): void

    /**
     * 取消监听地理位置
     */
    static subscribe(): void

}
