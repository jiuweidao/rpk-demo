// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * 页面路由
 * 接口声明: 无需声明
 */
export default class router {
    /**
     * 跳转到应用内的某个页面
     * @param obj 
     * @property uri `uri`参数默认策略处理逻辑支持 `schema` 是 `hap`
     * @property params 跳转时需要传递的数据，跳转到短信发送页面时，可以通过body（1040+）插入短信内容；跳转到快应用页面时，参数可以在页面中通过this.param1的方式使用，param1 为 json 中的参数名，param1 对应的值会统一转换为 String 类型
     */
    static push(obj: { uri: string, params: object }): void;

    /**
     * 跳转到应用内的某个页面，当前页面无法返回
     * @param obj
     * @property uri 要跳转到的uri
     * @property params 跳转时需要传递的数据，参数可以在页面中通过this.param1的方式使用，param1为json中的参数名，param1对应的值会统一转换为String类型
     */
    static replace(obj: { uri: string, params: object }): void;

    /**
     * 返回指定页面
     * @param obj
     * @property path 返回目标页面的路径
     */
    static back(obj: { path?: string }): void;

    /**
     * 清空所有历史页面记录，仅保留当前页面
     */
    static clear(): void;

    /**
     * 获取当前页面栈的页面数量
     * @returns 页面数量
     */
    static getLength(): number;

    /**
     * 获取当前页面状态
     */
    static getState(): { index: number, name: string, path: string };
}
