// Type definitions for Hap
// Definitions by: Hap <https://www.quickapp.cn/>
/**
 * 通知消息
 * 接口声明: {"name": "system.notification"}
 */
export default class notification {
    /**
     * 打开网页，标题栏样式与打开webview的页面的标题栏样式相同，在webview的useragent后追加内容，格式是hap/<平台版本号>/<厂商标识> <平台应用包名>/<平台应用版本号> <应用名>/<应用版本号> (<来源信息>)。“来源信息”与app接口的getInfo方法返回结果中的source字段相同。
     * @param obj
     * @property contentTitle 标题
     * @property contentText 内容
     * @property clickAction 通知点击后触发动作的信息
     */
    static show(obj: { contentTitle?: string, contentText?: string, clickAction?: { uri: string } }): void
}
